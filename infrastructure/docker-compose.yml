services:
  postgres:
    image: postgres:16
    container_name: axiomforge-postgres
    restart: unless-stopped
    environment: 
      POSTGRES_USER: axiomforge
      POSTGRES_PASSWORD: axiomforge
      POSTGRES_DB: axiomforge
    ports: 
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data


  redis: 
    image: redis:7
    container_name: axiomforge-redis
    restart: unless-stopped
    ports:
      - "6379:6379"

volumes: 
  postgres_data:

